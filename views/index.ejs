<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Al-Shorouk Radiology Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/custom.css" rel="stylesheet">
</head>
<body class="bg-light">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <i class="fas fa-hospital-alt me-2"></i>
                Al-Shorouk Radiology
            </a>

            <div class="d-flex align-items-center">
                <span class="text-white me-3">
                    <i class="fas fa-user me-1"></i>
                    Welcome, <%= user.fullName %> (<%= user.role %>)
                </span>
                <form action="/logout" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-outline-light btn-sm">
                        <i class="fas fa-sign-out-alt me-1"></i>Logout
                    </button>
                </form>
            </div>
        </div>
    </nav>

    <div class="container-fluid py-4">
        <div class="row justify-content-center">
            <div class="col-12 col-md-10 col-lg-8">
                <div class="text-center my-5">
                    <h1 class="display-4 text-dark mb-4">
                        <i class="fas fa-hospital-alt me-3"></i>
                        Radiology Management System
                    </h1>
                    <p class="lead text-muted mb-5">
                        Comprehensive healthcare forms for radiology department management
                    </p>
                </div>

                <div class="row g-4">
                    <% if (user.role === 'admin') { %>
                        <!-- Admin Options -->
                        <div class="col-12 col-md-6 col-lg-4">
                            <div class="card h-100 shadow-sm hover-card">
                                <div class="card-body text-center p-4">
                                    <div class="icon-wrapper mb-3">
                                        <i class="fas fa-users-cog fa-3x text-dark"></i>
                                    </div>
                                    <h3 class="card-title text-dark mb-3">User Management</h3>
                                    <p class="card-text text-muted mb-4">
                                        Create, edit, and manage system users including nurses, doctors, and administrators.
                                    </p>
                                    <a href="/admin" class="btn btn-warning btn-lg px-4">
                                        <i class="fas fa-users me-2"></i>
                                        Manage Users
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% } %>

                    <% if (user.role === 'nurse') { %>
                        <!-- Nurse Options -->
                        <div class="col-12 col-lg-3">
                            <div class="card h-100 shadow-sm hover-card">
                                <div class="card-body text-center p-4">
                                    <div class="icon-wrapper mb-3">
                                        <i class="fas fa-search fa-3x text-dark"></i>
                                    </div>
                                    <h3 class="card-title text-dark mb-3">Patient Search</h3>
                                    <p class="card-text text-muted mb-4">
                                        Search for patients by SSN and start new assessments immediately.
                                    </p>
                                    <a href="/nurse/search-patient" class="btn btn-primary btn-lg px-4">
                                        <i class="fas fa-search me-2"></i>
                                        Search Patient
                                    </a>
                                </div>
                            </div>
                        </div>

                        <!-- Current Visits Status for Nurses -->
                        <% if (currentVisits && currentVisits.length > 0) { %>
                            <div class="col-12 col-lg-9">
                                <div class="card h-100">
                                    <div class="card-header bg-primary text-white">
                                        <h5 class="card-title mb-0">
                                            <i class="fas fa-clipboard-list me-2"></i>Your Current Assessments
                                        </h5>
                                    </div>
                                    <div class="card-body">
                                        <div class="table-responsive">
                                            <table class="table table-hover mb-0">
                                                <thead class="table-light">
                                                    <tr>
                                                        <th><i class="fas fa-calendar me-1"></i>Visit Date</th>
                                                        <th><i class="fas fa-user me-1"></i>Patient</th>
                                                        <th><i class="fas fa-id-card me-1"></i>Medical #</th>
                                                        <th><i class="fas fa-stethoscope me-1"></i>Diagnosis</th>
                                                        <th><i class="fas fa-tasks me-1"></i>Status</th>
                                                        <th><i class="fas fa-cogs me-1"></i>Actions</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <% currentVisits.forEach(visit => { %>
                                                        <tr>
                                                            <td>
                                                                <div>
                                                                    <strong><%= new Date(visit.visit_date).toLocaleDateString() %></strong>
                                                                    <br>
                                                                    <small class="text-muted"><%= new Date(visit.visit_date).toLocaleTimeString() %></small>
                                                                </div>
                                                            </td>
                                                            <td>
                                                                <div>
                                                                    <strong><%= visit.patient_name %></strong>
                                                                    <br>
                                                                    <small class="text-muted">
                                                                        <%= visit.gender %>,
                                                                        <%= Math.floor((new Date() - new Date(visit.date_of_birth)) / (365.25 * 24 * 60 * 60 * 1000)) %> years old
                                                                    </small>
                                                                </div>
                                                            </td>
                                                            <td><%= visit.medical_number || '-' %></td>
                                                            <td>
                                                                <% if (visit.primary_diagnosis) { %>
                                                                    <div>
                                                                        <strong><%= visit.primary_diagnosis %></strong>
                                                                        <% if (visit.diagnosis_code) { %>
                                                                            <br><small class="text-muted">Code: <%= visit.diagnosis_code %></small>
                                                                        <% } %>
                                                                    </div>
                                                                <% } else { %>
                                                                    <span class="text-muted">-</span>
                                                                <% } %>
                                                            </td>
                                                            <td>
                                                                <div class="assessment-status">
                                                                    <% if (visit.assessment_id) { %>
                                                                        <% if (visit.is_draft) { %>
                                                                            <span class="badge bg-warning text-dark">
                                                                                <i class="fas fa-edit me-1"></i>Draft
                                                                            </span>
                                                                        <% } else { %>
                                                                            <span class="badge bg-success">
                                                                                <i class="fas fa-check-circle me-1"></i>Completed
                                                                            </span>
                                                                        <% } %>
                                                                    <% } else { %>
                                                                        <span class="badge bg-secondary">
                                                                            <i class="fas fa-exclamation-triangle me-1"></i>Not Started
                                                                        </span>
                                                                    <% } %>
                                                                </div>
                                                            </td>
                                                            <td>
                                                                <div class="btn-group btn-group-sm" role="group">
                                                                    <% if (visit.assessment_id) { %>
                                                                        <a href="/nurse/assessment/<%= visit.visit_id %>" class="btn btn-outline-primary" title="Continue Assessment">
                                                                            <i class="fas fa-edit"></i> Continue
                                                                        </a>
                                                                    <% } else { %>
                                                                        <a href="/nurse/assessment/<%= visit.visit_id %>" class="btn btn-success" title="Start Assessment">
                                                                            <i class="fas fa-play"></i> Start
                                                                        </a>
                                                                    <% } %>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    <% }); %>
                                                </tbody>
                                            </table>
                                        </div>
                                        <div class="mt-3 text-center">
                                            <a href="/nurse/my-assessments" class="btn btn-outline-primary btn-sm">
                                                <i class="fas fa-list me-1"></i>View All Assessments
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% } %>
                    <% } %>

                    <% if (user.role === 'physician') { %>
                        <!-- Doctor Options -->
                        <div class="col-12 col-md-6 col-lg-4">
                            <div class="card h-100 shadow-sm hover-card">
                                <div class="card-body text-center p-4">
                                    <div class="icon-wrapper mb-3">
                                        <i class="fas fa-user-md fa-3x text-dark"></i>
                                    </div>
                                    <h3 class="card-title text-dark mb-3">Physician Dashboard</h3>
                                    <p class="card-text text-muted mb-4">
                                        Access your radiology dashboard, manage assessments, and view patient reports.
                                    </p>
                                    <a href="/doctor" class="btn btn-success btn-lg px-4">
                                        <i class="fas fa-tachometer-alt me-2"></i>
                                        My Dashboard
                                    </a>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 col-md-6 col-lg-4">
                            <div class="card h-100 shadow-sm hover-card">
                                <div class="card-body text-center p-4">
                                    <div class="icon-wrapper mb-3">
                                        <i class="fas fa-x-ray fa-3x text-primary"></i>
                                    </div>
                                    <h3 class="card-title text-primary mb-3">Radiology Assessment</h3>
                                    <p class="card-text text-muted mb-4">
                                        Complete radiology assessment forms with technical parameters and findings.
                                    </p>
                                    <a href="/radiology-form" class="btn btn-primary btn-lg px-4">
                                        <i class="fas fa-microscope me-2"></i>
                                        Start Assessment
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% } %>
                </div>

                <div class="text-center mt-5">
                    <div class="alert alert-light border">
                        <h5 class="alert-heading">
                            <i class="fas fa-tablet-alt me-2"></i>
                            Tablet Compatible
                        </h5>
                        <p class="mb-0">
                            All forms are optimized for tablet devices with touch-friendly interfaces
                            and responsive design for seamless mobile healthcare workflows.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>